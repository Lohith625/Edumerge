# AI usage log — in progress

Tool: Codex (OpenAI).

## Requested assistance

1. Read the supplied assessment and compare project options.
2. Collaboratively design the support workflow, architecture, permissions, and scope.
3. Implement the agreed first milestone: login, request submission, claiming, and ownership visibility.

## Human decisions

The candidate selected Student Support with the assistant, confirmed staff self-claiming and manager oversight, approved role-based demo accounts and paused SLAs, supplied feature ideas, and approved incremental implementation.

## Generated code

Initial React interface, FastAPI endpoints, database models and seed data, API tests, and local setup documentation were generated by Codex. Human code modifications have not yet been recorded; do not claim them unless made.

## Validation

Executed: four API tests passed, including real concurrent claims, permissions, validation, and session revocation. Two Playwright tests passed in Microsoft Edge: the full student → staff → student workflow and mobile navigation/sign-out. The TypeScript check and Vite production build passed. Desktop and mobile screenshots were visually inspected. PostgreSQL runtime verification remains pending while Docker is unavailable.

## Useful prompt

“first lest design the system design and architercutre … do not build everthing by you … buiod with me”

## Wrong output and corrections

The initial mobile CSS hid the profile section, also hiding sign-out. Codex identified this during code review, restored the mobile profile controls, and added a browser assertion verifying logout returns to the login screen. The corrected browser test passed. This was an assistant correction, not a candidate code modification.

The first desktop browser test also used `Staff` as an exact accessible name, while the button text was `staff` (visually capitalized with CSS). The test timed out. Codex corrected the locator to match the actual accessible text and reran it successfully.

This report is a working log and must be completed with the candidate's own review before submission.

## Candidate review: usability improvements

The candidate reported missing or unclear back navigation, registration, small text, profile access, and staff claiming. Codex added student-only registration, self profiles, department-scoped student profiles for staff, explicit account switching, prominent back controls, larger text, and queue-level claim buttons. The candidate identified the claim discoverability problem; claiming previously required opening ticket details.

Validation after changes: five backend tests, three browser tests, and the production build passed. Browser coverage includes registration/login, profile/back navigation, queue claiming, and mobile sign-out. Desktop and mobile screenshots were reviewed.

## Candidate review: category-specific intake

The candidate identified that generic request descriptions would create avoidable staff follow-ups. Codex added structured, category-specific questions for attendance corrections, certificates, fees, and ID cards. Conditional fee and ID-card fields limit questions to the selected issue. The server independently validates required and conditional answers, and ticket details show labeled answers to authorized users.

## Candidate-approved completion work

After the candidate asked to continue and complete the agreed scope, Codex implemented the remaining collaboration decisions: public replies and private staff notes; waiting-for-student, resolution, and reopening states; separate SLA cycles with paused waiting time; manager escalations and notifications; department ageing and staff-workload views; manager reassignment, priority, and acknowledgement controls; and a rule-based request suggestion with a non-blocking same-student duplicate warning.

Validation after completion: eight backend tests, four browser workflows, and the production build were run. The browser workflows cover the complete student-to-staff resolution and reopen journey, mobile request creation, registration and queue claiming, and the manager ageing dashboard.

## Candidate review before submission

The candidate should read this log, run the project locally, and only claim work they understand and can explain. The implementation uses fictional demo data and includes no external AI model call; the request suggestion is intentionally transparent rule-based logic.
